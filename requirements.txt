# Core
numpy>=1.24.0
pandas>=2.0.0
scipy>=1.11.0

# Camera & Video
opencv-python>=4.8.0
mediapipe>=0.10.0

# Serial (Oximeter - Contec CMS50E; ESP32 CSI/EMG/GSR)
pyserial>=3.5

# Audio capture (recorder-owned mic on the master clock) — optional, lazy-imported
sounddevice>=0.4.6

# ECG over BLE (Polar H10 PMD stream) — optional, lazy-imported
bleak>=0.21.0

# Thermal screen-share capture (FLIR C5 via screen region) — optional, lazy-imported
mss>=9.0.0

# Deep Learning
torch>=2.0.0
torchvision>=0.15.0
torchaudio>=2.0.0

# Signal Processing
neurokit2>=0.2.0
heartpy>=1.2.5

# Visualization
matplotlib>=3.7.0
seaborn>=0.12.0
plotly>=5.15.0

# Notebooks
jupyter>=1.0.0
ipywidgets>=8.0.0

# Web Dashboard
flask>=3.0.0
flask-socketio>=5.3.0
simple-websocket>=1.0.0

# Utilities
pyyaml>=6.0
tqdm>=4.65.0
click>=8.1.0
rich>=13.0.0

# Testing
pytest>=7.4.0
